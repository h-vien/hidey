// Main content script entry point
// All modules are loaded via manifest.json in order:
// blur-engine.js, selector-detector.js, drag-blur.js, content.js
// Each module initializes itself when loaded

console.log('Hidey: Content script loaded');

